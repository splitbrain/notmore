<div>
    <h2>Thread {{ threadId|default('')|e }}</h2>

    {% if messages is empty %}
        <p>No messages available.</p>
    {% else %}
        {% include 'mail/thread_messages.html.twig' with {'messages': messages} %}
    {% endif %}
</div>
